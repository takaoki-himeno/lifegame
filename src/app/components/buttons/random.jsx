'use client'
import { styled } from '@mui/system'

export default function Random({ clickHandlerRandom }) {
  return (
    <>
        <button onClick={ clickHandlerRandom }>Random</button>
    </>
  )
}
